/** §8 Delivery Dispatch — orders grouped by delivery zone for rider */
export default function DeliveryDispatch() {
  return (
    <div className="p-6 max-w-5xl mx-auto">
      <h1 className="text-2xl font-display tracking-tight text-foreground">Delivery Dispatch</h1>
      <p className="text-sm text-muted-foreground mt-1">Group orders by zone for the selected delivery date.</p>
      <div className="mt-6 rounded-lg border border-border bg-card p-8 text-center text-muted-foreground">
        Date filter and zone-grouped order list with Mark Delivered will be implemented here.
      </div>
    </div>
  );
}
